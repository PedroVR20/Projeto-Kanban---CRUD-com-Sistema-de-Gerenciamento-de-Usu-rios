import { MaxYearDirective } from './max-year.directive';

describe('MaxYearDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxYearDirective();
    expect(directive).toBeTruthy();
  });
});
