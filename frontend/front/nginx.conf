server {
    listen 80;
    server_name localhost;

    # CORREÇÃO: Aponta a raiz para a subpasta 'browser'
    root /usr/share/nginx/html/browser;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
